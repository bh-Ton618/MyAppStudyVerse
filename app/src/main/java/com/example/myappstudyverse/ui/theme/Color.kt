package com.example.myappstudyverse.ui.theme

import androidx.compose.ui.graphics.Color

// Main surfaces
val SpaceBackground = Color(0xFF070B1A)
val SpaceSurface = Color(0xFF080F1E)
//Color(0xFF0D0C21) before color keep it for now !
// Color(0xFF12112A) 0xFF15142E

val NavigationSurface = Color(0xFF0C1328)

val LectureCardSurface = Color(0xFF171D46)

val PriorityLow = Color(0xFFB9A7C4)

// Accent colors
val PurplePrimary = Color(0xFF6C4DFF)
val PurpleSecondary = Color(0xFFB86CFF)

// Text colors
val OffWhite1 = Color(0xFFD9D7E2)
val TextSecondary = Color(0xFFB8BCD0)

val InputFieldText = OffWhite1

val TextLabel = Color(0xFFD9D7E2)
// Timetable / subtle UI elements
val Gridline = Color.White.copy(alpha = 0.04f)
val Gridline2 = Color.White.copy(alpha = 0.2f)

// unordered for now
val Rose = Color(0xFF8650CC)
val Green = Color(0xFF72C79A)
val Gold = Color(0xFFC2A45A)
val LighterGold = Gold.copy(alpha = 0.6f)

val textFieldInputHint = Color.White.copy(alpha = 0.25f)
val SaveAndDeleteButton = Color(0xFF7C4DFF)
val OffWhite2 = Color.White.copy(alpha = 0.7f)